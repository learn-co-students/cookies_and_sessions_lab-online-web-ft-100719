<h1>Your Cart</h1>
<ul>
  <% (session[:cart] || []).each do |product| %>
    <li><%= product %></li>
  <% end %>
</ul>

<%= form_for :product do |f| %>
  <%= f.label :product %>
  <%= text_field_tag(:product) %>
  <%= f.submit "add to cart" %>
<% end %>