<%= cart.each do |item| %>
<%= item %> <br>
<% end %>
<%= form_for :add, :url => add_path do |f| %>
<%= f.text_field :product, name:"product", id:"product" %>
<%= f.submit "add to cart" %>
<% end %>